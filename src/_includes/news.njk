{% extends "base.njk" %}

{% block content %}
<article>
  <h1>{{ title }}</h1>
  <p><small>{{ date | date("MMMM D, YYYY") }} by {{ author }}</small></p>
  <div>
    {{ content | safe }}
  </div>
</article>
{% endblock %}
