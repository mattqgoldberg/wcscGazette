---
title: "Meet the Council"
layout: base
permalink: "/team/"
---
<div class="team-page">
  <h1>{{ title }}</h1>
  <p>The Warren College Student Council is made up of dedicated students who work to represent and serve the Warren College community.</p>

  {% set councilMembers = collections.councilMembers %}
  
  {% if councilMembers.length > 0 %}
    <h2>Executive Officers</h2>
    <div class="members-list">
      {% for member in councilMembers %}
        {% if member.data.category == "Executive" %}
          {% include "council-card.njk" %}
        {% endif %}
      {% endfor %}
    </div>

    <h2>Elected Representatives</h2>
    <div class="members-list">
      {% for member in councilMembers %}
        {% if member.data.category == "Elected" %}
          {% include "council-card.njk" %}
        {% endif %}
      {% endfor %}
    </div>

    <h2>Appointed Positions</h2>
    <div class="members-list">
      {% for member in councilMembers %}
        {% if member.data.category == "Appointed" %}
          {% include "council-card.njk" %}
        {% endif %}
      {% endfor %}
    </div>

    <h2>Committee Members</h2>
    <div class="members-list">
      {% for member in councilMembers %}
        {% if member.data.category == "Committee" %}
          {% include "council-card.njk" %}
        {% endif %}
      {% endfor %}
    </div>

    <h2>Judicial Board</h2>
    <div class="members-list">
      {% for member in councilMembers %}
        {% if member.data.category == "Judicial Board" %}
          {% include "council-card.njk" %}
        {% endif %}
      {% endfor %}
    </div>
  {% else %}
    <div class="static-council-content">
      <h2>Executive Officers</h2>
      <ul>
        <li><strong>President:</strong> Aries Cole</li>
        <li><strong>Vice President Internal:</strong> Ghazal Saeedi</li>
        <li><strong>Vice President External:</strong> Julianna Ybarra</li>
        <!-- More static content if needed -->
      </ul>
    </div>
  {% endif %}

  <p>Interested in joining the Warren College Student Council? Contact us for information about available positions and elections.</p>
</div>
